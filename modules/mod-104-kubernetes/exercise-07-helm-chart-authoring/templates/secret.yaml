{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata: { name: {{ include "iris-api.fullname" . }} }
type: Opaque
stringData:
  {{- range $k, $v := .Values.secrets }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
{{- end }}
