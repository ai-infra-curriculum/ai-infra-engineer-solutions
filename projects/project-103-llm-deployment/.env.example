# LLM Configuration
MODEL_CONFIG=tiny-llama
EMBEDDING_MODEL=all-MiniLM-L6-v2
VECTOR_DB_BACKEND=chromadb

# Paths
CHROMA_PERSIST_DIR=./chroma_db
MODEL_CACHE_DIR=./models
LOGS_DIR=./logs

# RAG Settings
RAG_TOP_K=5
RAG_CHUNK_SIZE=512
RAG_CHUNK_OVERLAP=50

# Cost Tracking
GPU_COST_PER_HOUR=1.0
STORAGE_COST_PER_GB_MONTH=0.10
VECTOR_DB_COST_PER_QUERY=0.0001

# API Settings
HOST=0.0.0.0
PORT=8000
RATE_LIMIT_RPM=60
LOG_LEVEL=INFO

# Hugging Face (for model downloads)
HUGGING_FACE_TOKEN=

# Pinecone (if using Pinecone instead of ChromaDB)
PINECONE_API_KEY=
PINECONE_ENVIRONMENT=
PINECONE_INDEX_NAME=rag-documents

# Optional: API Authentication
API_SECRET_KEY=
JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
